# https://github.com/chocolatey/choco/wiki/CommandsList
# https://chocolatey.org/docs/commands-list

# line continuation: Type a <space>, '`' (backtick), ENTER/RETURN
choco search `
    docker `
    docker-for-windows `
    docker-desktop `
    docker-toolbox `


#cinst -y `
choco install -y `
    docker `
    docker-for-windows `
    docker-desktop `
    docker-toolbox `
